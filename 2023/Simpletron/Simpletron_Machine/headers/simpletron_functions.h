//
// Created by Magoimortal_br on 12/03/2023.
//

#ifndef SIMPLETRON_SIMPLETRON_FUNCTIONS_H
#define SIMPLETRON_SIMPLETRON_FUNCTIONS_H
/*
#include "simpletron_machine.h"

static bool read_instruction(simpletron *v1);
static void write_instruction(simpletron *v1);
static void load_instruction(simpletron *v1);
static void store_instruction(simpletron *v1);
static void add_instruction(simpletron *v1);
static void subtract_instruction(simpletron *v1);
static void multiply_instruction(simpletron *v1);
static enum exit_val divide_instruction(simpletron *v1);
static enum exit_val remainder_instruction(simpletron *v1);
static void branch_instruction(simpletron *v1);
static void branch_negative_instruction(simpletron *v1);
static void branch_zero_instruction(simpletron *v1);
static enum exit_val halt_instruction();

*/
#endif //SIMPLETRON_SIMPLETRON_FUNCTIONS_H
